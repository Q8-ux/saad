# Database migrations

Historical migrations and imported legal-source records are intentionally excluded from this source branch. They may contain operational data, OCR artifacts, or historic branding.

Use `db/schema.ts` to generate a clean migration set for each deployment environment. Do not commit production database exports or user data.
