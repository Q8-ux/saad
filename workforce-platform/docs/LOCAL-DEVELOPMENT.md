# Local development

```bash
cp .env.example .env
docker compose up --build
```

After startup, seed the database from the API container and sign in with the demonstration company ID and administrator credentials documented in the main README.
