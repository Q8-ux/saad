# Data model

The starter includes Company, User, Employee and Attendance entities. Every business record is scoped by `companyId` to support tenant isolation. Attendance records store event type, timestamp, optional coordinates, accuracy, device identifier and source.
