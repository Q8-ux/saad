'use client';
import {useEffect,useState} from 'react';
type Employee={id:string;employeeNo:string;fullNameAr:string;department?:string};
type Attendance={id:string;type:string;recordedAt:string;employee:Employee};
export default function Dashboard(){const [employees,setEmployees]=useState<Employee[]>([]);const [attendance,setAttendance]=useState<Attendance[]>([]);const api=process.env.NEXT_PUBLIC_API_URL!;const headers=()=>({Authorization:`Bearer ${localStorage.getItem('token')}`});async function load(){const [e,a]=await Promise.all([fetch(`${api}/employees`,{headers:headers()}),fetch(`${api}/attendance`,{headers:headers()})]);if(e.ok)setEmployees(await e.json());if(a.ok)setAttendance(await a.json())}useEffect(()=>{load()},[]);return <main><h1>لوحة التحكم</h1><div className="grid"><div className="card"><div>الموظفون</div><div className="metric">{employees.length}</div></div><div className="card"><div>حركات الحضور</div><div className="metric">{attendance.length}</div></div></div><div className="card" style={{marginTop:20}}><h2>آخر الحركات</h2><table><thead><tr><th>الموظف</th><th>الحركة</th><th>التوقيت</th></tr></thead><tbody>{attendance.slice(0,20).map(x=><tr key={x.id}><td>{x.employee.fullNameAr}</td><td>{x.type==='CHECK_IN'?'دخول':'خروج'}</td><td>{new Date(x.recordedAt).toLocaleString('ar-KW')}</td></tr>)}</tbody></table></div></main>}
