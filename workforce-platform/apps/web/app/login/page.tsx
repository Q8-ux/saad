'use client';
import {FormEvent,useState} from 'react';
import {useRouter} from 'next/navigation';
export default function Login(){const r=useRouter();const [error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const res=await fetch(`${process.env.NEXT_PUBLIC_API_URL}/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});const data=await res.json();if(!res.ok){setError(data.message??'فشل الدخول');return}localStorage.setItem('token',data.accessToken);r.push('/dashboard')}return <main><div className="card" style={{maxWidth:460,margin:'80px auto'}}><h1>تسجيل الدخول</h1><form onSubmit={submit}><input name="companyId" defaultValue="00000000-0000-0000-0000-000000000001" required/><input name="email" type="email" defaultValue="admin@example.com" required/><input name="password" type="password" defaultValue="ChangeMe123!" required/><button>دخول</button></form>{error&&<p>{error}</p>}</div></main>}
