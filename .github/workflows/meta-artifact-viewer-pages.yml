name: Deploy Nitaq Al Amal Dashboard

on:
  push:
    branches: [main]
    paths:
      - 'meta-artifact-viewer/**'
      - '.github/workflows/meta-artifact-viewer-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: meta-artifact-viewer/package-lock.json

      - name: Install dependencies
        working-directory: meta-artifact-viewer
        run: npm ci

      - name: Build dashboard
        working-directory: meta-artifact-viewer
        run: npm run build

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload built dashboard
        uses: actions/upload-pages-artifact@v3
        with:
          path: meta-artifact-viewer/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
