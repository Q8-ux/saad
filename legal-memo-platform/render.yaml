services:
  - type: web
    name: legal-memo-platform
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/health
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: OPENAI_MODEL
        value: gpt-4.1-mini
      - key: SESSION_SECRET
        generateValue: true
      - key: LEGAL_RESEARCH_API_TOKEN
        generateValue: true
      - key: INTERNAL_APP_PORT
        value: 3001
    disk:
      name: legal-memo-data
      mountPath: /var/data
      sizeGB: 5
